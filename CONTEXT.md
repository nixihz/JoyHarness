# Joy Harness Context

## 核心术语

- **Joy Harness**：将游戏手柄作为本地智能体工作流控制面的产品。
- **控制器端点**：系统发现的单个 Xbox、DualSense、DualShock、Joy-Con L 或 Joy-Con R 设备。
- **逻辑控制器**：当前接收输入的一组控制器端点；Joy-Con L/R 可以单独使用，也可以组合成一副手柄。
- **Joy-Con 握持方向**：单只 Joy-Con 的横握或竖握配置；成对模式使用固定的双手柄布局。
- **映射模式**：Joy Harness 将控制器输入解释为任务、指针和 Codex Micro 操作。
- **原生手柄模式**：Joy Harness 停止消费游戏输入，让前台游戏或应用直接使用控制器；可根据前台应用自动切换。
- **物理 Codex Micro 模式**：由外接 Codex Micro 接收任务控制操作的工作模式。
- **任务槽位**：六个固定位置中的一个；每个位置可关联一个任务，并显示自身状态。
- **选中槽位**：当前唯一获得控制操作目标的任务槽位。
- **任务状态**：`idle`、`busy`、`waiting`、`done` 或 `error`；它描述任务的当前可见状态。
- **映射**：控制器输入与操作之间的用户配置关系。
- **功能层**：按住 LT/L2 后可使用的第二组控制器输入。
- **自定义映射**：用户明确设置的映射；它优先于产品默认值。
- **本地状态输入**：本机自动化向 Joy Harness 提交任务状态或槽位操作的入口。

## 领域边界

Joy Harness 负责控制器端点发现与组合、输入模式切换、任务槽位呈现、状态反馈和本地任务控制。它不拥有游戏输入处理、任务执行、任务内容、权限决策策略或远端账号状态。原生手柄模式下，游戏输入由前台应用拥有；任务名称和任务状态是外部工作流提供的事实，Joy Harness 只显示和转发它们。

## 生命周期

应用启动后建立一个可操作的六槽位工作台，并以空槽位、`idle` 和映射模式作为初始可见状态。控制器端点、外接控制设备和本地状态输入可以在运行期间连接、断开、重新组合或恢复；前台应用变化可以触发映射模式与原生手柄模式切换。应用停止后不应继续保留可操作的本地控制面。

## 不变量

- 始终存在六个任务槽位，编号为 1 到 6。
- 任一时刻恰有一个选中槽位。
- 槽位状态必须属于定义的五种任务状态之一；无法识别的状态按 `idle` 呈现。
- 自定义映射在产品升级后保持不变，除非用户主动重置或修改。
- 每个控制器家族保留独立映射；Joy-Con L、Joy-Con R 和 Joy-Con Pair 互不覆盖。
- 一个物理 Joy-Con 端点最多属于一个当前逻辑控制器；未选中的重复端点保持 inactive。
- 原生手柄模式不应触发 Joy Harness 映射操作；返回映射模式后恢复当前逻辑控制器映射。
- 控制操作的“已接纳”只表示已进入本地处理流程，不表示目标任务或外接设备已经完成该操作。
