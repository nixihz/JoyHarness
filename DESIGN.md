# Joy Harness 界面设计指南

这是 Joy Harness 的界面设计指南。任何新增页面、重做组件或调整视觉样式，都先回答：它是否让用户更快看懂当前控制状态，是否保留了 Joy Harness 的触感和节制感。

这份文件规定设计方向、取舍原则和界面语言。它不是功能规格、协议说明或截图验收报告。具体实现可以变化，但不能破坏这里定义的产品气质和视觉秩序。

## 一句话方向

**Joy Harness 是一座安静、可靠、带有游戏触感的 macOS 控制驾驶舱。**

用户大部分时间不应该“管理软件”，而应该把注意力放在当前任务和手里的控制器上。界面负责把复杂的连接、映射和权限压缩成清楚的状态，把必要的下一步放在手边。

## 品牌气质

Joy Harness 同时具备四个特征：

- **Tactile / 有触感**：控制器图、按键高亮和短促反馈让实体输入在屏幕上留下痕迹。
- **Calm / 安静**：大面积留白、低对比材质和稳定布局，避免控制台看起来像告警墙。
- **Precise / 精确**：映射、设备能力和权限信息排列严谨，数值和状态不能含糊。
- **Playful / 有一点玩心**：青色动能线、轻微的彩色高亮和控制器图允许表达个性，但只在关键瞬间出现。

设计应该让人感觉“我在操控一个可靠的工具”，而不是“我在浏览一个游戏主题网页”。

## 视觉核心

### 让控制器成为主角

控制器是 Joy Harness 最重要的视觉对象。控制器图应该清楚、居中、有足够呼吸空间；按键高亮是它的反馈层，不要在周围堆叠卡片、徽章或装饰性图案。

当没有真实控制器图时，使用 SF Symbol 的 gamecontroller.fill 作为安静的替代，不要临时绘制风格不一致的插图。

### 用一处动能带动整屏

每个界面只需要一个主要强调色。默认使用 macOS accent color；品牌表达可以使用青色作为控制器输入和活动状态的点缀，柔和紫色只用于微弱的品牌光晕或辅助高亮。

不要同时使用多种渐变、霓虹边框和大面积彩色背景。Joy Harness 的能量应该像按键触发时的一道短光，而不是永久发光的舞台。

### 层次来自材质和间距

优先使用 macOS 的系统背景、regularMaterial、ultraThinMaterial 和系统分隔线建立层次。卡片不靠粗边框和重阴影浮起来。

界面层次按这个顺序组织：

1. 页面背景。
2. 侧栏或检查器的材质层。
3. 当前任务、状态横幅和控制映射的工作层。
4. 按键高亮、模式横条和错误提示的反馈层。

同一层里的元素应该共享背景和间距，不要把每一行都包装成独立小卡片。

## 色彩语言

品牌资产可见的颜色关系是：**墨蓝承载主体，暖象牙提供呼吸，青色表示输入能量，紫色提供少量灵气**。界面实现优先使用 macOS 语义色，以确保亮色和深色外观都可读。

| 用途 | 视觉方向 | 使用方式 |
| --- | --- | --- |
| 页面与材质 | 系统背景、墨蓝的低对比印象 | 作为容器和背景，不做纯品牌海报 |
| 主要输入 | 青色 / accent | 按键按下、选中、活动状态、关键动作 |
| 品牌微光 | 柔和紫色 | 控制器中心或品牌图形的微弱高亮 |
| 成功 | green | 已连接、已完成、可用 |
| 注意 | orange | 等待批准、需要检查、部分能力缺失 |
| 错误 | red | 错误、权限阻断、不可继续 |
| 次要信息 | secondary / tertiary | 设备详情、解释、版本号 |

状态颜色必须同时配合文字、图标或形状。不要用整块红色或绿色背景表达连接状态，也不要让品牌青色承担错误含义。

## 字体和文字

- 使用 macOS 系统字体，保持原生设置窗口的可信度。
- 标题用 semibold 建立层级；正文保持正常字重；诊断值、Bundle ID 和版本号可以使用 monospaced。
- 不使用装饰性标题字体，不用全大写制造“科技感”，不使用负字距。
- 文案说用户要做什么，不说系统内部怎么实现。使用“选择应用”“恢复默认映射”“打开声音输入设置”，不要使用“写入服务”“刷新 HID 状态”。
- 一个控件只承担一个动作，动作名称在页面、菜单、确认框和结果提示中保持一致。
- 错误文案说明发生了什么以及下一步怎么修复；空状态给出行动方向，不只写“暂无数据”。
- 简体中文和 English 是同一套设计，不为英文单独改变信息结构。中文变长时允许换行，不能压缩成难以阅读的固定宽度。

## 布局原则

### 稳定的三栏工作台

Dashboard 使用“槽位—任务—诊断”的三栏关系：

- 左侧让用户选择当前任务。
- 中间让用户理解和执行当前任务。
- 右侧让用户确认连接与能力。

这三个区域的职责不能互换。诊断信息不能挤进主任务区，任务操作不能被隐藏在检查器里。窗口缩放时优先保留中间工作区，侧栏和检查器保持足够的可读宽度。

### 设置是分类，不是迷宫

Settings 使用标准 macOS 侧边栏和分组表单。导航只放稳定的任务类别：通用、按键映射、槽位快捷键、原生模式。不要为了单个开关新增导航项，也不要把相关设置拆到多个弹窗。

页面内按用户决策顺序组织：先解释影响，再放控件，最后放恢复或添加动作。设置立即生效时，不要再放“保存”按钮。

### 留白是功能

留白用于区分任务、状态和反馈。使用 4 pt 基线，常用 8 / 12 / 16 / 22 pt 间距；卡片和检查器保持约 16 pt 内边距，工作区保持更宽的呼吸空间。

圆角保持克制，卡片默认约 7 pt。不要用不同圆角表达没有意义的层级，也不要为了填满窗口增加装饰模块。

## 核心组件语言

### 任务槽位

槽位是一个持续存在的选择器，不是通知列表。

- 编号、任务标题、任务状态和状态点按固定顺序排列。
- 选中态使用低透明度 accent 背景，保持整行可点击。
- 任务标题可以省略，状态名称不能消失。
- 槽位状态点要小而稳定，不使用跳动动画吸引注意。
- 快捷键是辅助信息，通过 help 或 caption 呈现，不抢占任务标题空间。

### 状态横幅

状态横幅只做一件事：告诉用户当前任务处于什么阶段。

- 状态图标、名称和简短解释在同一视觉组中。
- idle、busy、waiting、done、error 使用固定的图标与语义色。
- “等待批准”和“发生错误”必须比普通运行状态更容易被发现，但不能覆盖整个页面。
- 状态变化可以短暂淡入；不能用持续闪烁或声音替代清晰文字。

### 控制映射

映射界面要让用户建立“手上哪个按键 → 屏幕执行什么”的关系。

- 物理输入名和映射动作分开显示，先显示用户认识的按键名。
- 控制器图放在中间，按键标签围绕它排列；不要把它变成普通表格。
- 按下时使用青色或系统 accent 高亮，松开后快速恢复。
- 四个方向键在遥控器上独立出现；可独立配置的输入不能合并成模糊摘要。
- 单只 Joy-Con 的握持方向使用原生 segmented control；不要用插图暗示当前方向。
- 映射动作选择使用原生 Picker；复杂动作的附加配置紧跟在该动作下面。

### 链路状态

链路状态是“可信度面板”，不是日志窗口。

- 只展示帮助用户判断能否继续的信息：控制器、适配器、运行模式、权限、语音输入和震动能力。
- 每个分组有标题、图标、连接状态和简短值；长值允许换行。
- “已连接”“未连接”“已授权”“未授权”要使用一致的词语。
- 未知值显示“未知”或不显示该行，不把未知伪装成 0%、关闭或错误。
- 震动测试属于低频工具动作，固定放在检查器底部，不与核心诊断混在一起。

### 按钮、菜单和确认

- 首选原生 macOS button style；主要动作使用一个视觉重点，其他动作退到 bordered、borderless 或 link。
- 图标按钮必须有可见的 help 和 accessibility label。
- 恢复默认、删除应用等操作使用 destructive role，并说明影响范围。
- 弹窗只处理需要确认、选择文件或输入 Bundle ID 的短流程；不要把整个设置页面塞进 sheet。
- 不使用自定义 Toast 传达关键状态。关键结果应出现在触发动作的附近，并且能被再次读到。

## 动效与触感

动效只服务于因果关系：

- 按键高亮使用约 120 ms 的短促 ease-out，让用户知道输入已经被接收。
- 选中槽位和模式切换可以使用轻微淡入或颜色过渡，不能延迟操作。
- 连接状态不使用循环呼吸、持续抖动或大范围粒子效果。
- 尊重 Reduce Motion；关闭动效后，状态和焦点仍然完整可读。
- 真实震动是硬件反馈，界面只需要提供清晰的测试入口和结果，不模拟过度复杂的屏幕效果。

## 原生模式的表达

映射模式和原生手柄模式是两个清晰的工作状态。

进入原生模式时：

- 在控制映射附近显示明显但克制的模式横条。
- 写清楚映射已暂停、当前前台应用是谁、如何切回。
- 控制器图可以保留，但不能继续表现出映射动作正在执行。

回到映射模式时，模式横条消失，按键高亮和映射反馈恢复。不要只在检查器里改变一行文字，让用户自己猜主界面发生了什么。

## 资产使用

品牌资产位于 Sources/JoyHarness/Resources/Brand/：

- joy-harness-logo-concept-v5.png 是高分辨率 Logo 母版。
- joy-harness-logo-readme.png 是 README 展示图。
- joy-harness-app-icon-v5.png 和 JoyHarness.icns 是应用图标资产。
- 控制器 Dashboard 图位于 Sources/JoyHarness/Resources/。

Logo 和应用图标用于品牌入口、关于信息和分发资产。不要把 Logo 当作每个设置页的装饰，也不要重新绘制控制器图来替代现有资产。新增插图必须与墨蓝、暖象牙、青色和柔和紫色的关系一致。

## 明确禁止

- 不做通用 SaaS 仪表盘风格：不要 KPI 卡片墙、彩色统计砖块、粗重阴影和无意义的圆角。
- 不做游戏 HUD 风格：不要满屏霓虹、血条、持续闪烁、拟物纹理或科幻网格。
- 不做网页风格设置页：不要自定义 tab bar、营销式 hero、巨大渐变标题或非 macOS 的表单控件。
- 不把所有信息都提升为同一等级；诊断详情必须服从当前任务。
- 不用颜色单独表达状态，不用图标单独表达动作，不用缩写替代用户能理解的词。
- 不为“看起来更完整”添加无行为的装饰、空卡片、假数据或占位徽章。
- 不在没有真实视觉目标时随意引入新的字体、图标风格或视觉主题。

## 新界面设计流程

开始写代码前，先在设计说明或 PR 描述中写清楚：

1. 这个界面服务哪个用户任务，完成后用户应该知道或能做什么。
2. 它属于 Dashboard 还是 Settings，应该放在哪个现有区域。
3. 主要信息、次要信息和反馈信息分别是什么。
4. 选择了哪一个视觉重点，为什么不会与状态色冲突。
5. 空状态、断开、错误、等待和成功状态怎么表达。
6. 中文和 English 哪些文案会变长，布局如何容纳。
7. 是否复用了现有组件、材质、间距和品牌资产。

然后用真实内容检查亮色、深色、中文、English、键盘焦点、VoiceOver 和 Reduce Motion。若一个设计需要额外解释才能看懂，先删掉装饰并重新整理层级。

## 参考实现边界

当前 macOS SwiftUI 实现提供了可复用的基线：

- Dashboard：最小 980 × 680 pt，默认 1240 × 820 pt，三栏结构。
- Settings：620 × 640 pt，170 pt 侧边栏，分组表单。
- 常用间距：4 / 8 / 12 / 16 / 22 pt。
- 常用卡片圆角：约 7 pt。
- 状态色：secondary、cyan、orange、green、red。
- 系统图标：SF Symbols。
- 深色和亮色：优先系统语义色和材质。

这些是当前实现的落点，不是要求每个新界面机械复制的模板。新设计可以调整尺寸，但必须说明它如何保持相同的信息层级、品牌气质和 macOS 原生感。
