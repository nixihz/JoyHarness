# Joy Harness 界面设计规范

这份文档是 Joy Harness 的界面设计与实现契约。新增页面、组件重构、状态补充和 AI 生成代码审查都必须遵守它。它定义产品气质、信息层级、状态表达和可访问性基线；具体实现可以变化，但不能破坏这些约束。

开始设计前先回答一个问题：**用户能否在 0.5 秒内看懂当前控制器是否可用、输入模式和下一步动作？** 如果不能，先减少装饰和信息噪声，再增加功能。

## 1. 产品定位

**Joy Harness 是一个安静、可靠、带有游戏实体触感的 macOS 控制器工具。**

用户的注意力应留在手里的控制器和当前应用上。界面把控制器端点、逻辑控制器、映射模式和权限压缩成明确的可操作信息，并把必要的下一步放在手边。

四个品牌气质必须同时成立：

- **Tactile / 有触感**：控制器图、按键高亮和短促反馈让实体输入在屏幕上留下确定痕迹。
- **Calm / 安静**：留白、系统材质和稳定布局优先，避免把控制台做成告警墙。
- **Precise / 精确**：映射、设备能力、权限和时序值使用一致术语，未知值不能伪装成正常值。
- **Playful / 有一点玩心**：青色动能线和柔和紫色只在关键瞬间点缀，不能变成持续发光的舞台。

## 2. 设计 Token 契约

所有颜色、间距、圆角、动效和状态表达都通过 Token 或已有系统语义值使用。业务视图中禁止直接散落 Hex、RGB、任意间距和动画时长。

### 2.1 三层 Token

| 层级 | 责任 | 示例 | 使用规则 |
| --- | --- | --- | --- |
| **Primitive** 基础 Token | 保存不可变的色板、尺寸和时序原子值 | `color.cyan.500`、`space.4`、`motion.keyRelease` | 只被语义层引用，不直接进入业务视图 |
| **Semantic** 语义 Token | 表达产品意图并映射亮色、深色和高对比外观 | `surface.canvas`、`content.primary`、`status.error` | 组件优先使用这一层，优先选择 macOS 语义色 |
| **Component** 组件 Token | 固定某个组件的状态、层级和交互契约 | `controller.selectedSurface`、`key.activeGlow` | 只描述组件差异，不能重新发明全局色板 |

新增 Token 时必须说明所属层级、亮暗色映射、对比度结果和使用场景。无法归入现有语义的值先复核设计，再决定是否新增。

### 2.2 Primitive 基础色板

基础色板只定义品牌原子值；业务代码不得直接引用这些值。亮色/深色的最终可读性由语义层控制。

| Primitive Token | Light 值 | Dark 值 | 备注 |
| --- | --- | --- | --- |
| `color.cyan.600` | `#0891B2` | `#22D3EE` | 输入高亮和通信脉冲 |
| `color.purple.400` | `#9333EA` | `#C084FC` | 品牌弱光晕，低透明度 |
| `color.green.600` | `#16A34A` | `#4ADE80` | 成功状态 |
| `color.orange.600` | `#D97706` | `#FBBF24` | 注意状态 |
| `color.red.600` | `#DC2626` | `#F87171` | 阻断状态 |
| `space.4` / `space.8` / `space.12` | `4 / 8 / 12 pt` | `4 / 8 / 12 pt` | 间距基线 |
| `radius.card` | `7 pt` | `7 pt` | 卡片默认圆角 |

### 2.3 颜色语义

品牌关系是**墨蓝承载主体、暖象牙提供呼吸、青色表示输入能量、柔和紫色提供少量灵气**。实现优先使用 `Color.accentColor`、`Color.primary`、`Color.secondary`、系统背景和材质。

| 语义 Token | 视觉方向 | 用途 | 约束 |
| --- | --- | --- | --- |
| `surface.canvas` | 系统窗口背景 / 低对比墨蓝 | 页面和工作区底层 | 不使用纯品牌海报式大色块 |
| `surface.material` | `regularMaterial` / `ultraThinMaterial` | 按需展开的详情、分组容器 | 不依赖重阴影制造层次 |
| `action.primary` | 系统 accent | 选中、主要动作、焦点 | 每个界面只保留一个视觉重点 |
| `input.kinetic` | 青色 | 物理按键按下、输入脉冲 | 只表达输入能量，不表达错误 |
| `brand.glow` | 柔和紫色 | 控制器中心或品牌图形弱光晕 | 低不透明度、短暂出现 |
| `status.success` | 系统 green | 已连接、已完成、可用 | 必须同时有文字和图标 |
| `status.attention` | 系统 orange | 需要授权、部分能力缺失 | 必须说明下一步 |
| `status.blocked` | 系统 red | 错误、断开、权限阻断 | 不使用整屏红色背景 |
| `content.muted` | `secondary` / `tertiary` | 设备详情、版本、解释 | 不能承担核心信息 |

## 3. WCAG 2.2 AA 与 macOS 无障碍基线

无障碍是状态契约的一部分，亮色、深色和高对比模式都必须验证。

- 正文和核心状态文字与背景的对比度至少 **4.5:1**；大字号文字至少 **3:1**。
- 图标、边框、焦点环等非文字关键界面组件与相邻颜色至少 **3:1**。禁用控件可以降低对比度，但必须保持可辨识。
- 任何状态都不能只用颜色表达，至少同时提供语义图标和文字；形状、位置或纹理可作为第三通道。
- 键盘焦点始终可见，不能被选中背景、材质或动效吞掉；焦点环与相邻背景至少 **3:1**。
- 交互目标保持至少 `24 × 24 pt` 的可操作区域；相邻控件之间要有足够间隔，避免误触。
- 所有图标按钮提供可见帮助文本和 VoiceOver `accessibilityLabel`；装饰图形标记为非访问元素。
- 不依赖鼠标完成映射：`Tab` / `Shift + Tab` 遍历、`Space` 激活、方向键调整选择，顺序与视觉阅读顺序一致。
- 支持 VoiceOver：控制器名称、按键、连接状态、运行模式、映射动作和阻断原因都可被连续读出。
- 尊重 Reduce Motion；关闭动画后，状态、焦点和反馈仍然完整可读。
- 中文和 English 共用信息结构。文本变长时允许换行或扩展宽度，禁止截断核心状态和动作名称。

## 4. 字体、文案与空间

### 4.1 字体阶梯

使用 macOS 系统字体和 Dynamic Type 语义样式，避免装饰字体、全大写标题和负字距。

| 层级 | SwiftUI 样式 | 用途 |
| --- | --- | --- |
| H1 | `.largeTitle`，semibold | Dashboard 控制器状态或页面标题 |
| H2 | `.title2`，semibold | 设备分组、详情一级分组 |
| H3 | `.title3`，medium | 设备名称、弹框动作指引 |
| Body | `.body`，regular | 交互标签、参数说明、正文 |
| Meta | `.caption`，regular | 快捷键、设备型号、副标题 |
| Diagnostic | `.system(.body, design: .monospaced)` | 延迟、HID 值、Bundle ID、版本号 |

### 4.2 文案契约

- 面向用户任务，不暴露内部协议：使用“重新扫描控制器”，不使用“刷新 HID 状态”。
- 错误说明发生了什么、影响是什么、用户下一步做什么：例如“辅助功能权限未开启。打开系统设置以允许 Joy Harness 模拟按键。”
- 空状态必须给出第一步动作，不只写“暂无数据”。
- 一个控件只承担一个动作；页面、菜单、确认框和结果提示使用同一动作名称。
- `已连接`、`未连接`、`已授权`、`未授权` 等状态词保持全局一致。
- 未知值显示“未知”或隐藏该行，不能显示成 `0%`、`关闭` 或错误。

### 4.3 空间 Token

以 `4 pt` 为基线，使用 `4 / 8 / 12 / 16 / 22 / 32 pt`。卡片和详情容器常用 `16 pt` 内边距；工作区提供更宽的呼吸空间；卡片默认圆角约 `7 pt`。不为填满窗口增加无行为的卡片或模块。

## 5. 布局和信息层级

### 5.1 Dashboard：控制器与输入

主界面用于确认控制器是否可用、查看实体输入反馈和进入映射设置。任务在目标应用中查看和操作，Joy Harness 界面不展示任务列表、任务槽位、任务标题、任务进度或任务状态横幅，也不提供任务选择与跳转区。

- **控制器是主视觉**：显示当前控制器及对应图解，按键高亮反映真实输入；没有设备时显示连接指引。
- **连接与模式清楚可见**：提供简短的连接、运行模式和权限摘要。影响操作的阻断及修复入口放在相关内容附近。
- **映射设置容易进入**：从控制器附近进入 Settings 配置按键，预览中区分物理输入和映射动作。
- **详情按需出现**：设备能力、权限明细和测试工具通过明确入口展开，不占用常驻诊断侧栏。
- **尺寸随内容确定**：采用紧凑的单一主内容区，允许长文案换行和内容滚动，不为三栏布局或任务展示预留宽度。

任务槽位和任务状态仍可作为底层控制与硬件反馈的领域数据存在；这不构成在界面展示它们的理由。设置中的槽位快捷键仅用于配置输入，不展示关联任务或实时任务状态。

### 5.2 Settings

Settings 使用标准 macOS 侧边栏和分组表单，导航只放稳定的任务类别：通用、按键映射、槽位快捷键、原生模式。页面按“影响说明 → 控件 → 恢复或添加动作”组织；设置立即生效时不增加“保存”按钮。

### 5.3 麦克风组件启用

“通用 → 遥控器麦克风”的启用按钮直接安装应用自带组件，不打开独立安装器。
按钮附近说明管理员验证及音频短暂中断；等待期间禁用重复安装和切换输入，显示原生进度指示。
完成后提示音频服务重载和选择输入的下一步；取消、失败均就近显示真实结果，不提前声称设备可用。
复用表单、系统按钮、caption 和 secondary 样式，中英文提示允许换行。

## 6. 连接与能力状态契约

界面状态回答“控制器能否使用、哪些能力可用、受阻时如何恢复”。下表描述展示语义，不新增或替换底层协议状态。任务状态的五种领域值由 `CONTEXT.md` 定义，不映射为任务列表、徽章或横幅。

| 情况 | 展示内容 | 颜色 Token | 动作或说明 |
| --- | --- | --- | --- |
| 未连接 | 未连接控制器 | `content.muted` | 提供设备连接指引 |
| 连接中 | 正在连接 | `action.primary` | 根据真实连接过程更新，不提前显示成功 |
| 可用 | 已连接及当前输入模式 | `status.success` | 可以查看输入反馈或配置映射 |
| 部分能力不可用 | 具体缺失能力及影响 | `status.attention` | 提供相应授权或设置入口，保留可用功能 |
| 操作受阻 | 断开、权限阻断或操作失败的具体原因 | `status.blocked` | 说明受影响的操作和恢复方式 |
| 能力未知 | 未知或尚未读取 | `content.muted` | 不伪装成不可用、关闭或零值 |

状态表达包含图标和文字；受阻时补充原因与恢复动作。已连接不等于全部权限已授予，也不等于映射动作执行成功。

### 6.1 状态变化与反馈

- 连接和权限变化同步更新主界面摘要、已展开详情和 VoiceOver 文本，关键阻断不能只藏在详情中。
- 断开时清除残留的按键高亮，保留用户映射配置；重新连接后根据真实状态恢复显示。
- 进入原生手柄模式时明确映射已暂停，不显示映射动作仍在执行。
- 本地操作反馈放在触发位置附近；“已接纳”只表示进入本地处理流程，不表示目标应用已经完成操作。
- 不因收到任务状态更新而增加任务通知、进度区或任务横幅。

## 7. 核心组件契约

### 7.1 设备概览

设备名称、连接状态、当前输入模式与控制器图组成一个清晰的视觉组。设备不可用时给出实际原因或连接指引，不显示虚构电量、延迟或能力。

### 7.2 状态提示

提示用于说明连接、权限和模式变化。图标、状态名称和简短解释位于同一视觉组；普通状态保留为摘要，需要处理的阻断才使用醒目的就近提示。禁止持续闪烁或用声音替代文字。

### 7.3 控制器和映射

- 在映射预览和输入测试中，控制器图是主视觉对象，使用现有资源；未知控制器退回 `gamecontroller.fill`，不加载风格冲突的临时插图。
- 物理输入名与映射动作分开显示，先显示用户认识的按键名；可独立配置的输入不能合并成摘要。
- 按下事件在一帧内（目标 `<16 ms`）出现 `input.kinetic` 或 accent 高亮；抬起后按动效预算恢复。
- 单只 Joy-Con 的横握/竖握使用原生 segmented control；映射动作选择使用原生 Picker。

### 7.4 连接摘要与按需详情

主界面显示连接、运行模式和阻断摘要；控制器端点、逻辑控制器、适配器、权限明细、语音输入和震动能力放在按需展开的连接详情中。每组包含标题、图标、状态和简短值；长值允许换行。震动测试放在详情内，与连接摘要区分。

### 7.5 按钮和危险确认

优先使用原生 macOS button style。一个页面只保留一个主要视觉动作，其余使用 `bordered`、`borderless` 或 link。恢复默认、清除映射、解除绑定和删除应用使用 destructive role，并在确认框中说明影响范围。

## 8. 动效预算与触觉

动画只表达因果关系，不用于装饰和持续吸引注意。

| 触发 | 动效 | 时长上限 | 曲线 | 验收目的 |
| --- | --- | ---: | --- | --- |
| 物理按键按下 | Idle → Cyan 硬切换 | `0 ms` | linear | 输入零延迟可见 |
| 物理按键抬起 | Cyan → Idle 淡出 | `120 ms` | ease-out | 确认释放并保留机械余温 |
| 状态横幅出现 | 下滑 + 渐显 | `200 ms` | ease-out | 让新状态可被发现 |
| 原生模式挂起 | 横条展开 + 图层变暗 | `250 ms` | standard | 告知控制权转移 |

禁止弹性过冲、循环呼吸、持续抖动、大范围粒子和超过预算的页面过渡。开启 Reduce Motion 后：关闭位移、渐显和复杂过渡；按键使用 `0 ms` 纯色切换；焦点、文字和错误说明保持不变。

手柄震动只允许由连接详情中的“测试震动”或用户配置的 Haptic Pulse 主动触发；悬停和普通菜单点击不能触发震动。

## 9. 原生手柄模式

映射模式和原生手柄模式是两个清晰的工作状态。进入原生模式时，在主界面的连接摘要附近显示模式提示；打开映射预览时同步显示：说明“映射已暂停”、当前前台应用和恢复方式；控制器图可以保留，但不能继续表现映射动作正在执行。退出后横条消失，映射反馈恢复。

## 10. 资产和实现边界

品牌资产位于 `Sources/JoyHarness/Resources/Brand/`，控制器 Dashboard 图位于 `Sources/JoyHarness/Resources/`。Logo 和应用图标只用于品牌入口、关于信息和分发资产；不要把 Logo 当作设置页装饰，也不要重绘现有控制器图。

现有代码的参考尺寸与样式（不代表已完成本规范的布局调整）：

- Dashboard 尺寸根据控制器概览、连接摘要和输入反馈确定；不为任务展示区或常驻诊断区预留空间。
- Settings 约 `620 × 640 pt`，侧边栏约 `170 pt`。
- 常用间距 `4 / 8 / 12 / 16 / 22 pt`，卡片圆角约 `7 pt`。
- 系统语义色、SF Symbols、亮色/深色材质优先。

新尺寸根据内容和可读性验证，保留品牌气质与 macOS 原生感；不得为了复用历史尺寸保留空栏。

## 11. 明确禁止

- 禁止 KPI 卡片墙、彩色统计砖块、粗重阴影和无意义圆角，避免通用 SaaS 仪表盘化。
- 禁止满屏霓虹、血条、持续闪烁、拟物纹理和科幻网格，避免游戏 HUD 化。
- 禁止自定义 tab bar、营销式 hero、巨大渐变标题和非原生表单控件。
- 禁止用单一颜色、单一红点或单一图标表达状态或动作。
- 禁止假数据、无行为徽章、空卡片和为“看起来更完整”添加的装饰。
- 禁止把诊断详情提升到与控制器连接和输入反馈相同的视觉等级。
- 禁止添加任务列表、槽位卡片、任务状态横幅或任务跳转区，包括折叠或隐藏在详情中的任务展示。

## 12. 新界面准入与 PR 自检

开始写代码前，在设计说明或 PR 描述中写清楚：

1. 服务哪个用户目标，完成后用户应该知道或能做什么。
2. 属于 Dashboard 的控制器概览、按需详情还是 Settings，为什么需要常驻或展开。
3. 主要信息、次要信息和反馈信息分别是什么。
4. 视觉重点是什么，为什么不会与状态色冲突。
5. 空状态、断开、错误、等待和成功如何表达。
6. 中文和 English 哪些文案会变长，布局如何容纳。
7. 复用了哪些 Token、组件、材质、间距和品牌资产。

提交前逐项检查：

- [ ] 界面聚焦控制器、输入和配置，没有任务列表、任务槽位、任务状态展示或任务跳转区。
- [ ] 连接、模式和阻断信息清楚可见，详情按需展开，没有常驻三栏布局。
- [ ] 没有在业务视图硬编码 Hex、RGB、任意间距或动画时长。
- [ ] 亮色、深色和高对比模式下正文对比度均达到 WCAG 2.2 AA。
- [ ] 状态同时包含图标、文字和必要动作；未知值没有被伪装。
- [ ] `Tab`、`Shift + Tab`、`Space` 和方向键可以完成核心映射流程。
- [ ] VoiceOver 能读出控制器、连接状态、运行模式、映射动作和阻断原因。
- [ ] Reduce Motion 关闭位移和复杂过渡后，状态和焦点仍然可读。
- [ ] 按键反馈在一帧内出现，抬起淡出不超过 `120 ms`。
- [ ] 原生手柄模式明确显示控制权转移，不能继续触发映射操作。
- [ ] 中文和 English 的真实长文案没有截断核心信息。
- [ ] 没有新增无行为装饰、假数据或与现有语言冲突的视觉主题。
